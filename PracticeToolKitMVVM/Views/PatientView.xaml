<UserControl x:Class="PracticeToolKitMVVM.Views.PatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PracticeToolKitMVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Form -->
        <StackPanel Grid.Column="0" Margin="10">

            <TextBlock Text="Patient Form"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <TextBlock Text="Patient ID"/>
            <TextBox Text="{Binding Id}" Height="30" Margin="0,5"/>

            <TextBlock Text="Patient Name"/>
            <TextBox Text="{Binding PatientName}" Height="30" Margin="0,5"/>

            <TextBlock Text="Age"/>
            <TextBox Text="{Binding Age}" Height="30" Margin="0,5"/>

            <TextBlock Text="Gender"/>
            <TextBox Text="{Binding Gender}" Height="30" Margin="0,5"/>

            <TextBlock Text="Disease"/>
            <TextBox Text="{Binding Disease}" Height="30" Margin="0,5"/>

            <TextBlock Text="Doctor Name"/>
            <TextBox Text="{Binding DoctorName}" Height="30" Margin="0,5"/>

            <Button Content="Add Patient"
                    Command="{Binding AddPatientCommand}"
                    Height="35"
                    Margin="0,15,0,5"/>

            <Button Content="Update Patient"
                    Command="{Binding UpdatePatientCommand}"
                    Height="35"
                    Margin="0,5"/>

            <Button Content="Delete Patient"
                    Command="{Binding DeletePatientCommand}"
                    Height="35"
                    Margin="0,5"/>
        </StackPanel>

        <!-- Right DataGrid -->
        <DataGrid Grid.Column="1"
                  ItemsSource="{Binding Patients}"
                  SelectedItem="{Binding SelectedPatient}"
                  AutoGenerateColumns="False"
                  Margin="20,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding PatientName}" Width="*"/>
                <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="80"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="100"/>
                <DataGridTextColumn Header="Disease" Binding="{Binding Disease}" Width="150"/>
                <DataGridTextColumn Header="Doctor" Binding="{Binding DoctorName}" Width="150"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
